# 公廟網站 — 操作流程手冊

## 一、專案結構

```
temple-website/
├── backend/          ← Node.js + Express + SQLite 後端 API
│   ├── server.js     ← 伺服器入口
│   ├── db.js         ← 資料庫初始化
│   ├── routes/       ← API 路由
│   ├── middleware/   ← JWT 驗證中介層
│   └── uploads/      ← 上傳的圖片存放處
└── frontend/         ← React 18 + Vite + Tailwind CSS 前端
    └── src/
        ├── pages/        ← 前台頁面
        └── pages/admin/  ← 後台管理頁面
```

---

## 二、啟動服務

### 2-1 第一次啟動前（安裝相依套件）

```bash
# 安裝後端套件
cd C:\Users\L11311001\temple-website\backend
npm install

# 安裝前端套件
cd C:\Users\L11311001\temple-website\frontend
npm install
```

### 2-2 日常啟動（每次開機後）

需要**同時開兩個終端機視窗**：

**終端機 A — 後端**
```bash
cd C:\Users\L11311001\temple-website\backend
npm run dev
```
> 成功訊息：`✅ 資料庫初始化完成` + nodemon 監聽中

**終端機 B — 前端**
```bash
cd C:\Users\L11311001\temple-website\frontend
npm run dev
```
> 成功訊息：`VITE ready` + `Local: http://localhost:5173/`

### 2-3 服務位址

| 服務 | 網址 |
|------|------|
| 前台網站 | http://localhost:5173/ |
| 後台管理 | http://localhost:5173/admin/login |
| 後端 API  | http://localhost:3001/api/ |

---

## 三、常見問題排除

### Port 3001 已被佔用（EADDRINUSE）

原因：上次後端沒有正常關閉，舊的 node process 仍在背景執行。

**解法：** 在 PowerShell 執行：
```powershell
# 查詢佔用 Port 3001 的 PID
netstat -ano | Select-String ":3001 "

# 將 <PID> 換成查到的數字，例如 27428
Stop-Process -Id <PID> -Force
```
之後重新執行 `npm run dev` 即可。

### 前端 PostCSS 語法錯誤（Unexpected token 'export'）

原因：`postcss.config.js` 使用 ES module 語法，但 Node 以 CommonJS 載入。

**解法（已修復）：** 已將設定檔改名為 `postcss.config.cjs`，使用 `module.exports = {}` 語法，不需再處理。

---

## 四、後台管理系統使用流程

### 4-1 登入後台

1. 開啟瀏覽器，前往 **http://localhost:5173/admin/login**
2. 輸入帳號密碼：

| 欄位 | 預設值 |
|------|--------|
| 帳號 | `admin` |
| 密碼 | `admin123` |

3. 點選「登入」，自動跳轉到後台儀表板

> **建議：** 登入後立即至「網站設定」→「修改密碼」更換密碼

---

### 4-2 後台頁面說明

登入後左側選單包含以下功能：

```
後台選單
├── 儀表板        ← 統計卡片（輪播數、最新消息數、活動數、報名數）
├── 輪播管理      ← 首頁大圖輪播新增/編輯/刪除
├── 最新消息      ← 消息文章新增/編輯/刪除/上下架
├── 活動管理      ← 活動新增/編輯/刪除（含日期、地點）
├── 報名資料      ← 查看民眾報名記錄、更新狀態
└── 網站設定      ← 廟名、電話、地址、簡介、修改密碼
```

---

### 4-3 各功能操作流程

#### 新增消息文章
```
後台選單 → 最新消息 → 點選「新增消息」
→ 填寫標題、內容
→ 選擇是否上傳封面圖片
→ 勾選「發布」（否則存為草稿）
→ 點選「儲存」
```

#### 新增活動
```
後台選單 → 活動管理 → 點選「新增活動」
→ 填寫活動名稱、說明、開始/結束日期、地點
→ 選擇是否上傳活動圖片
→ 點選「儲存」
→ 活動會顯示於前台活動頁並提供報名連結
```

#### 管理輪播圖
```
後台選單 → 輪播管理
→ 點選「新增輪播」
→ 上傳圖片（限 JPG/PNG/GIF/WEBP，最大 5MB）
→ 填寫標題與說明（可留空）
→ 設定排序順序（數字小的排前面）
→ 點選「儲存」
```

#### 查看與處理報名資料
```
後台選單 → 報名資料
→ 可依狀態篩選：待確認 / 已確認 / 已取消
→ 點選報名記錄旁的狀態下拉選單，可切換狀態
→ 如需刪除，點選「刪除」按鈕
```

#### 修改網站基本資料
```
後台選單 → 網站設定
→ 修改廟名、電話、地址、開放時間、廟宇簡介
→ 點選「儲存設定」
```

#### 修改管理員密碼
```
後台選單 → 網站設定 → 捲動到下方「修改密碼」區塊
→ 輸入目前密碼
→ 輸入新密碼（輸入兩次確認）
→ 點選「修改密碼」
```

---

## 五、前台頁面說明

| 路由 | 頁面 | 說明 |
|------|------|------|
| `/` | 首頁 | 輪播圖、廟宇簡介、最新消息、活動預覽 |
| `/about` | 關於本廟 | 廟宇歷史與禮儀說明 |
| `/news` | 最新消息 | 所有已發布的消息列表 |
| `/news/:id` | 消息詳情 | 單篇消息全文 |
| `/activities` | 活動資訊 | 活動列表 |
| `/register` | 活動報名 | 民眾報名表（姓名、電話、活動選擇） |
| `/contact` | 聯絡我們 | 廟址、電話、Google Maps |

---

## 六、技術規格

| 項目 | 版本/技術 |
|------|-----------|
| 前端框架 | React 18 + Vite 5 |
| UI 樣式 | Tailwind CSS 3 |
| 後端框架 | Node.js + Express |
| 資料庫 | SQLite（better-sqlite3） |
| 身份驗證 | JWT（有效期 24 小時） |
| 圖片上傳 | Multer（存至 `/backend/uploads/`） |
| 密碼加密 | bcryptjs |

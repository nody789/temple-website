# 公廟網站啟動說明

## 前置需求

請先安裝 **Node.js v18 以上**（目前開發使用 v20）：
- 下載：https://nodejs.org/zh-tw/download
- 安裝後在終端機確認：`node --version`

---

## 第一次安裝（只需執行一次）

打開兩個終端機視窗：

### 步驟一：建立環境變數檔案

在 `temple-website/backend/` 資料夾建立 `.env` 檔案，內容如下：

```
PORT=3001
JWT_SECRET=temple-secret-key-change-this-in-production
```

> **上線前請務必將 JWT_SECRET 換成隨機強密碼**，詳見 `HOSTING_GUIDE.md`。

### 步驟二：安裝後端
```
cd temple-website/backend
npm install
```

### 步驟三：安裝前端
```
cd temple-website/frontend
npm install
```

---

## 每次啟動

### 終端機 1：啟動後端（API 伺服器）
```
cd temple-website/backend
npm run dev
```
後端會在 http://localhost:3001 執行

### 終端機 2：啟動前端（React）
```
cd temple-website/frontend
npm run dev
```
前端會在 http://localhost:5173 執行

---

## 使用方式

- **前台網站**：http://localhost:5173
- **後台管理**：http://localhost:5173/admin/login
  - 預設帳號：`admin`
  - 預設密碼：`admin123`（請登入後立即更改！）

---

## 資料庫

- 使用 SQLite，檔案位置：`backend/temple.db`
- 可以用 Navicat Premium Lite 開啟這個 .db 檔案查看資料
- 首次啟動會自動建立所有資料表和範例資料

---

## 功能說明

### 前台
- 首頁：輪播 + 簡介 + 最新消息 + 活動 + 報名號召
- 本廟簡介：廟宇介紹、入廟須知
- 最新消息：公告清單 + 詳細內容
- 活動訊息：活動列表
- 線上報名：報名表格
- 聯絡我們：地址、電話、地圖

### 後台（需登入）
- 輪播管理：上傳/刪除/排序首頁輪播圖片
- 消息管理：新增/編輯/刪除最新消息
- 活動管理：新增/編輯/刪除活動
- 報名記錄：查看所有線上報名，可更改狀態
- 網站設定：修改廟名、電話、地址、簡介等文字，以及管理員密碼

---

## 資料夾結構

```
temple-website/
├── backend/           ← Node.js + Express API
│   ├── routes/        ← 各功能的 API 路由
│   ├── middleware/    ← 登入驗證中介層
│   ├── uploads/       ← 上傳的圖片存這裡
│   ├── db.js          ← 資料庫設定
│   └── server.js      ← 伺服器主程式
└── frontend/          ← React + Vite + Tailwind
    └── src/
        ├── pages/     ← 各頁面
        ├── components/← 可重複使用的元件
        ├── api/       ← 與後端溝通的 API 函式
        └── context/   ← 登入狀態管理
```

---

## 上線部署

請參閱 [HOSTING_GUIDE.md](./HOSTING_GUIDE.md) — 包含網域申請、Railway 部署、DNS 設定完整流程。
